// @flow

import * as React from "react"

type Props = {|
  locationId: ?string
|}

const LocationDetail = ({ locationId }: Props) => {
  return <div>TODO {locationId}</div>
}

export default LocationDetail
