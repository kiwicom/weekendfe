import React, { useRef, useEffect } from "react"
import InputField from "@kiwicom/orbit-components/lib/InputField"

const Example = () => {
  const ref = useRef(null)
  useEffect(() => {
  })
  return <InputField placeholder="Hello World" />
}
export default Example
